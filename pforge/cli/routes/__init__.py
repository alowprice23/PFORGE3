"""
This package manages the CLI's knowledge of its own capabilities and
its memory of past operations.

- registry.yaml: A static manifest of all available skills.
- learned.sqlite: The constellation memory database (not a source file).
"""
