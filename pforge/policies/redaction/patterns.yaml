# Redaction Patterns

# Regular expressions for common secret formats
regex:
  - '(?i)api_key["'\'_]?\s*[:=]\s*["'\'_]?([a-zA-Z0-9_]+)'
  - '(?i)password["'\'_]?\s*[:=]\s*["'\'_]?([a-zA-Z0-9_]+)'

# Denylist of file paths and directory names
denylist:
  - '.ssh/'
  - '.aws/'
  - 'credentials.json'
